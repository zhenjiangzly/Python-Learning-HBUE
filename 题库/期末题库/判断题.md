# 判断题（50 题）

> 来源：湖北经济学院 Python 期末题库（超星平台导出）
> 
> ⚠️ 答案为平台原参考答案，**仅供参考**，如有疑问欢迎提交 Issue/PR 指正。

## 题目

**1.** Python程序必须经过编译生成字节码后才能执行。

- [ ] 正确
- [ ] 错误

**2.** Tensorflow不属于机器学习领域第三方库

- [ ] 正确
- [ ] 错误

**3.** matplotlib属于Python数据分析领域第三方库

- [ ] 正确
- [ ] 错误

**4.** True和False都是python语句的常量。

- [ ] 正确
- [ ] 错误

**5.** 开源理念不属于Python语言的特点。

- [ ] 正确
- [ ] 错误

**6.** 顺序结构程序每行代码都会执行且仅执行一次。

- [ ] 正确
- [ ] 错误

**7.** 在Python中，对代码进行注释只有一种方式，那就是使用#符号。

- [ ] 正确
- [ ] 错误

**8.** 用print函数输出字符串时，只能使用单引号（'）。

- [ ] 正确
- [ ] 错误

**9.** 用print函数输出字符串时，可以使用单引号（'）、双引号（"）或三引号（"'）。

- [ ] 正确
- [ ] 错误

**10.** Python运算符%不但可以用来进行求余运算，还可以用来格式化字符串。

- [ ] 正确
- [ ] 错误

**11.** 在Python中，input函数返回的是数值类型的数据。

- [ ] 正确
- [ ] 错误

**12.** 在Python 3.x中，input函数把用户的键盘输入作为字符串返回。

- [ ] 正确
- [ ] 错误

**13.** 注释是对代码的解释说明，因此注释对程序的执行是起作用的。

- [ ] 正确
- [ ] 错误

**14.** 注释是对代码的解释说明，但是注释对程序的执行不起作用。

- [ ] 正确
- [ ] 错误

**15.** Python使用缩进来体现代码之间的逻辑关系。

- [ ] 正确
- [ ] 错误

**16.** if语句会判断if后面的逻辑表达式，当表达式为真时，执行if后续的语句块

- [ ] 正确
- [ ] 错误

**17.** 使用and连接两个条件时，只要有一个条件为真，整个条件表达式就为真。

- [ ] 正确
- [ ] 错误

**18.** elif和else可以单独使用，不一定要与if配合使用。

- [ ] 正确
- [ ] 错误

**19.** 仅当条件为真时才会执行单分支选择结构中的操作。

- [ ] 正确
- [ ] 错误

**20.** 在if-elif-else多分支语句中，只会执行一个分支中的语句块。

- [ ] 正确
- [ ] 错误

**21.** 在if-elif-else结构中，else子句是可选的。

- [ ] 正确
- [ ] 错误

**22.** if语句可以同时进行多个条件判断。使用or进行连接时，只要其中一个条件成立，则整个条件表达式为True。

- [ ] 正确
- [ ] 错误

**23.** 在Python中，else子句一定跟离它最近的if配对。

- [ ] 正确
- [ ] 错误

**24.** 所有 if 语句中必须要有 else子句，否则会出现逻辑不完整的错误。

- [ ] 正确
- [ ] 错误

**25.** 在 Python 中，if语句可以没有 else 子句。

- [ ] 正确
- [ ] 错误

**26.** 创建只包含一个元素的元组时，必须在元素后面加一个逗号，例如(3,)。

- [ ] 正确
- [ ] 错误

**27.** (3)可以创建只有一个元素3的元组类型。

- [ ] 正确
- [ ] 错误

**28.** Python元组中的元素在创建后可以被重新赋值。

- [ ] 正确
- [ ] 错误

**29.** 元组可以通过append()方法添加新元素。

- [ ] 正确
- [ ] 错误

**30.** 元组是不可变数据类型，可以作为字典的键。

- [ ] 正确
- [ ] 错误

**31.** 使用tuple()函数可将列表转换为元组。

- [ ] 正确
- [ ] 错误

**32.** 使用list()函数可将元组转换为列表。

- [ ] 正确
- [ ] 错误

**33.** 定义元组时使用圆括号，而定义列表时使用方括号。

- [ ] 正确
- [ ] 错误

**34.** 元组变量初始化后，元组中元素的值无法进行修改。

- [ ] 正确
- [ ] 错误

**35.** 元组提供count(x)方法统计元素x出现的次数。

- [ ] 正确
- [ ] 错误

**36.** 可以用in运算符测试字典中是否存在某个键，但不能测试是否存在某个值。

- [ ] 正确
- [ ] 错误

**37.** 字典的“键”可以是字符串、元组类型。

- [ ] 正确
- [ ] 错误

**38.** 字典中的“键”必须是不可变类型。

- [ ] 正确
- [ ] 错误

**39.** 字典中的“键”允许重复。

- [ ] 正确
- [ ] 错误

**40.** 字典中的“值”不允许重复。

- [ ] 正确
- [ ] 错误

**41.** Python支持使用字典的“键”作为下标来访问字典中的值。

- [ ] 正确
- [ ] 错误

**42.** 语句 'age' in my_dict.keys() 可以检查键 'age' 是否存在于字典中。

- [ ] 正确
- [ ] 错误

**43.** 字典的键和值的数量必须相等。

- [ ] 正确
- [ ] 错误

**44.** 字典的元素之间以分号分隔。

- [ ] 正确
- [ ] 错误

**45.** 字典的“键”和“值”之间以冒号分隔

- [ ] 正确
- [ ] 错误

**46.** random. sample (pop, k)返回从pop类型中随机选取的k个元素构成的列表

- [ ] 正确
- [ ] 错误

**47.** 内置函数与标准库函数一样使用前需要先导入所属库。

- [ ] 正确
- [ ] 错误

**48.** 用random(a, B.产生一个[0.0, 1.0)之间的随机小数

- [ ] 正确
- [ ] 错误

**49.** 第三方库函数在使用前需要先下载到开发环境中。

- [ ] 正确
- [ ] 错误

**50.** 函数可以分为内置函数与标准库函数这两类。

- [ ] 正确
- [ ] 错误


## 参考答案

| 题号 | 答案 |
|------|------|
| 1 | 错误 |
| 2 | 错误 |
| 3 | 正确 |
| 4 | 正确 |
| 5 | 错误 |
| 6 | 正确 |
| 7 | 错误 |
| 8 | 错误 |
| 9 | 正确 |
| 10 | 正确 |
| 11 | 错误 |
| 12 | 正确 |
| 13 | 错误 |
| 14 | 正确 |
| 15 | 正确 |
| 16 | 正确 |
| 17 | 错误 |
| 18 | 错误 |
| 19 | 正确 |
| 20 | 正确 |
| 21 | 正确 |
| 22 | 正确 |
| 23 | 错误 |
| 24 | 错误 |
| 25 | 正确 |
| 26 | 正确 |
| 27 | 错误 |
| 28 | 错误 |
| 29 | 错误 |
| 30 | 正确 |
| 31 | 正确 |
| 32 | 正确 |
| 33 | 正确 |
| 34 | 正确 |
| 35 | 正确 |
| 36 | 正确 |
| 37 | 正确 |
| 38 | 正确 |
| 39 | 错误 |
| 40 | 错误 |
| 41 | 正确 |
| 42 | 正确 |
| 43 | 正确 |
| 44 | 错误 |
| 45 | 正确 |
| 46 | 正确 |
| 47 | 错误 |
| 48 | 错误 |
| 49 | 正确 |
| 50 | 错误 |