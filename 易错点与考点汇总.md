# ⚠️ 易错点与考点汇总

> 根据二级 Python 历年高频失分点整理。建议考前通读 2~3 遍，并在刷题时对照检查。

## 1. 基础语法类

| 易错点 | 错误写法 | 正确写法 |
|--------|----------|----------|
| input() 返回值当数字用 | `age + 1` | `int(input()) + 1` |
| 标识符以数字开头 | `2name = 1` | `name2 = 1` |
| 保留字当变量 | `class = 1` | `cls = 1` |
| 忘记缩进 | if 后下一行不缩进 | if 块内统一缩进 |
| 认为 print 是保留字 | —— | print/input/type/range 都是**内置函数** |

## 2. 运算类（高频）

- `/` 永远返回**浮点数**：`4/2 = 2.0`（选择题常考 type）
- `//` 是向下取整：`-7//2 = -4`（不是 -3！）
- `%` 符号跟随除数：`-7%2 = 1`、`7%-2 = -1`
- `**` 右结合：`2**3**2 = 2**(3**2) = 512`（不是 64！）
- `0.1+0.2` 不等于 `0.3`（浮点误差）
- and/or 返回**参与运算的值**而非 True/False：`3 or 5 = 3`，`0 and 5 = 0`

## 3. 字符串类（必考）

- 切片**含头不含尾**：`"Hello"[0:4] = "Hell"`
- 负索引从 -1 开始：`"abc"[-1] = "c"`
- `(1)` 是整数，`(1,)` 才是元组
- `"a"*3 = "aaa"`；`"a"+3` 会报 TypeError（不能字符串加数字）
- `str(123)` 与 `int("123")` 方向别搞反
- `"%d" % 3.7` 是旧格式化，考试用 format()/f-string
- `chr(97) = 'a'`，`ord('a') = 97`（大小写差 32）

## 4. 控制结构类

- `range(a, b)` 不含 b；`randint(a, b)` 含 b
- 循环被 break 打断时，**else 不执行**；正常结束才执行
- while 循环体内**必须修改条件变量**，否则死循环
- `continue` 跳过本次，`break` 跳出整个循环
- `1 if True else 2` 的条件表达式，True 分支在前

## 5. 函数类

- 函数内只能**读**全局变量，**改**必须 `global`
- 默认参数必须放在非默认参数**之后**：`def f(a, b=1)` ✅
- 无 return 返回 None；`return` 后代码不执行
- 递归必须有**结束条件**，否则 RecursionError
- 列表是可变对象：函数内 `lst.append()` 会改变原列表；字符串不可变：函数内 `s = s + "!"` 不会改变外部
- `f(a=1, 2)` 语法错误：位置参数必须在关键字参数前

## 6. 组合数据类型类

- 列表可变、元组不可变（`t[0]=x` 报 TypeError）
- `d["键不存在"]` 报 KeyError；`d.get("键不存在")` 返回 None
- `sort()` 修改原列表，`sorted()` 返回新列表
- `[[0]*3]*3` 是同一行的引用（深坑！用列表推导式）
- 字典的键必须是**不可变类型**（列表、字典、集合不行）
- 遍历字典默认遍历**键**；要键值对用 `.items()`
- `remove(x)` 按值删，`pop(i)` 按索引删

## 7. 文件类

- `"w"` 会**清空**原文件，`"a"` 追加
- `write()` **不自动加换行**，手动加 `\n`
- 中文文件必须 `encoding="utf-8"`
- 读 CSV 后每行是**字符串**列表，数字要 int()/float() 转换
- 写 CSV 用 `newline=""` 避免空行
- 文件用完必须 close（或用 with）

## 8. 库类

- `random.randint(a, b)` **含 b**；`random.randrange(a, b)` **不含 b**
- `%m` 是月份，`%M` 是分钟（大写分钟！）
- `time.strftime(格式, 时间)` 参数顺序不能反
- turtle 正多边形转角 = 360/n；五角星是 144°
- `pip install` 在**命令行**运行，不是 Python 代码
- `jieba.lcut()` 返回**列表**

## 9. 公共基础知识（10 分）

- 数据结构：栈（后进先出）、队列（先进先出）、链表、二叉树
- 排序：冒泡、选择、插入、快速排序的比较次数
- 算法复杂度：时间复杂度和空间复杂度概念
- 数据库：关系模型、SQL 基础（SELECT/INSERT/UPDATE/DELETE）
- 软件工程：生命周期（需求分析→设计→编码→测试→维护）
- 网络安全：防火墙、加密、病毒特征

> 💡 这 10 分需要另外找公共基础知识的资料复习，本仓库暂不覆盖。
